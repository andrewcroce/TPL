(function($){

  // Initialize carousel function, called in app.js
	$.initGalleryCarousel = function(){

    $('.gallery-carousel').slick({
      // Add custom settings here (see http://kenwheeler.github.io/slick/)
      dots: true
    });

  }

})(jQuery);
